# Source: mawkingbird. Custom-domain host: mastodon_mock; github.io host: mawkingbird.
---
name: Deploy Mawkingbird Canary and Test
on:
  workflow_dispatch:
  push:
    branches: [main]

concurrency:
  group: gh-pages-branch
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ vars.MAWKINGBIRD_PUBLISH_ENABLED == 'true' }}
    name: Build and deploy Canary
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Configure production host deploy key
        env:
          PAGES_DEPLOY_KEY: ${{ secrets.MASTODON_MOCK_PAGES_DEPLOY_KEY }}
        run: bash .github/scripts/configure-pages-key.sh
      - name: Stamp build info
        run: node ui/scripts/gen-build-info.mjs
      - name: Build Canary
        run: make mockingbird MOCKINGBIRD_BASE_HREF=/canary/
      - name: Stamp Canary Bluesky OAuth metadata
        run: node ui/scripts/write-bluesky-oauth-metadata.mjs ui/dist-mockingbird/browser https://mawkingbird.com/canary/
      - name: Add canary marker
        run: printf '%s\n' "$GITHUB_SHA" > ui/dist-mockingbird/browser/canary-commit.txt
      - name: Publish /canary/ to gh-pages
        env:
          PUBLISH_REMOTE: git@github.com:matthewdeanmartin/mastodon_mock.git
        run: bash .github/scripts/publish-gh-pages.sh canary ui/dist-mockingbird/browser

  test-deployment:
    if: ${{ vars.MAWKINGBIRD_PUBLISH_ENABLED == 'true' }}
    name: Build and deploy /test/
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    needs: deploy
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Configure production host deploy key
        env:
          PAGES_DEPLOY_KEY: ${{ secrets.MASTODON_MOCK_PAGES_DEPLOY_KEY }}
        run: bash .github/scripts/configure-pages-key.sh
      - name: Stamp build info
        run: node ui/scripts/gen-build-info.mjs
      - name: Build /test/
        run: make mockingbird MOCKINGBIRD_BASE_HREF=/test/
      - name: Stamp test Bluesky OAuth metadata
        run: node ui/scripts/write-bluesky-oauth-metadata.mjs ui/dist-mockingbird/browser https://mawkingbird.com/test/
      - name: Add test marker
        run: printf '%s\n' "$GITHUB_SHA" > ui/dist-mockingbird/browser/test-commit.txt
      - name: Verify this really is a test build
        run: |
          grep --fixed-strings '<base href="/test/"/>' ui/dist-mockingbird/browser/index.html
          test ! -e ui/dist-mockingbird/browser/CNAME
      - name: Publish /test/ to gh-pages
        env:
          PUBLISH_REMOTE: git@github.com:matthewdeanmartin/mastodon_mock.git
        run: bash .github/scripts/publish-gh-pages.sh test ui/dist-mockingbird/browser

  mirror:
    if: ${{ vars.MAWKINGBIRD_PUBLISH_ENABLED == 'true' }}
    name: Build and deploy github.io Canary
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
      pages: write
    env:
      MIRROR_BASE_HREF: /mawkingbird/canary/
      MIRROR_REPOSITORY: matthewdeanmartin/mawkingbird
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ui/package-lock.json
      - name: Stamp build info
        run: node ui/scripts/gen-build-info.mjs
      - name: Build github.io Canary
        run: make mockingbird MOCKINGBIRD_BASE_HREF="$MIRROR_BASE_HREF"
      - name: Stamp mirror Canary Bluesky OAuth metadata
        run: node ui/scripts/write-bluesky-oauth-metadata.mjs ui/dist-mockingbird/browser https://matthewdeanmartin.github.io/mawkingbird/canary/
      - name: Add canary marker
        run: printf '%s\n' "$GITHUB_SHA" > ui/dist-mockingbird/browser/canary-commit.txt
      - name: Verify mirror Canary layout
        run: |
          grep --fixed-strings '<base href="/mawkingbird/canary/"/>' ui/dist-mockingbird/browser/index.html
          test ! -e ui/dist-mockingbird/browser/CNAME
      - name: Publish github.io /canary/
        env:
          PUBLISH_PUBLIC_BASE: /mawkingbird/canary/
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/scripts/publish-gh-pages.sh canary ui/dist-mockingbird/browser
      - name: Request mirror Pages build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh api --method POST repos/matthewdeanmartin/mawkingbird/pages/builds
